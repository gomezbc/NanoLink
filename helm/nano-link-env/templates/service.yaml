apiVersion: v1
kind: Service
metadata:
  name: {{ include "nano-link.fullname" . }}
  labels:
    {{- include "nano-link.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nano-link.selectorLabels" . | nindent 4 }}
